---
phase: 01-scraper-schema
plan: 05
type: execute
wave: 1
depends_on: ["01-04"]
files_modified:
  - scraper/strategies.py
  - .env
autonomous: false
gap_closure: true
requirements:
  - SCRP-01
  - SCRP-02
  - SCRP-03
  - SCRP-04
  - SCRP-05
  - SCRP-06
  - SCRP-07
  - SCRP-08
  - DB-01
  - DB-02
  - DB-03
  - DB-04
  - DB-05

must_haves:
  truths:
    - "run_scrape.py completes without error and SQLite file has non-zero rows in BOTH api_docs AND strategies tables"
    - "Strategy rows contain name, category, code_content, and description populated from the 经典策略学习 sidebar"
    - "Running scraper a second time produces identical row counts with no duplicates"
    - "Scraper loads persisted session from auth.json on second run without re-executing login"
  artifacts:
    - path: ".env"
      provides: "Valid JoinQuant web credentials (phone number format)"
      contains: "JQ_USERNAME="
    - path: "scraper/strategies.py"
      provides: "Strategy extraction with confirmed selectors"
    - path: "jq_knowledge.db"
      provides: "Populated SQLite database with non-zero rows in all tables"
  key_links:
    - from: "auth.py"
      to: ".env"
      via: "os.environ JQ_USERNAME and JQ_PASSWORD"
      pattern: "os\\.environ"
    - from: "run_scrape.py"
      to: "scraper/strategies.py"
      via: "scrape_all_strategies(context)"
      pattern: "scrape_all_strategies"
    - from: "scraper/strategies.py"
      to: "db/seed.py"
      via: "run_scrape.py upserts each strategy record"
      pattern: "upsert_strategy"

user_setup:
  - service: joinquant
    why: "Strategy page scraping requires valid JoinQuant web login (phone number format, not JQData API UUID)"
    env_vars:
      - name: JQ_USERNAME
        source: "Your Chinese mobile phone number registered at joinquant.com (e.g. 13800000000)"
      - name: JQ_PASSWORD
        source: "Your JoinQuant web platform password"
    dashboard_config:
      - task: "Register or log in at joinquant.com to confirm you have a valid web account"
        location: "https://www.joinquant.com"
---

<objective>
Close the 2 verification gaps from 01-VERIFICATION.md: strategy scraping produces 0 rows, and strategy data is unpopulated. Both gaps share a single root cause -- JoinQuant web login requires Chinese mobile phone credentials, and .env only has UUID API keys.

Purpose: All Phase 1 code is implemented, tested (29 tests pass), and wired. The ONLY blocker is a credential configuration issue. This plan provides valid credentials, runs the pipeline end-to-end, and confirms selectors work against the live authenticated DOM. If selectors need updating after seeing the real strategy page DOM, the auto task handles that.

Output: A fully populated jq_knowledge.db with non-zero rows in all 4 tables (api_docs, api_params, api_return_attrs, strategies), and confirmed session reuse via auth.json.
</objective>

<execution_context>
@/Users/jimmymacmini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jimmymacmini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scraper-schema/01-VERIFICATION.md
@.planning/phases/01-scraper-schema/01-02-SUMMARY.md
@.planning/phases/01-scraper-schema/01-04-SUMMARY.md
@.planning/phases/01-scraper-schema/SELECTORS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provide valid JoinQuant web credentials</name>
  <what-built>
    The complete Phase 1 scraper pipeline is implemented and all 29 tests pass. The only blocker is that .env contains UUID-format JQData API credentials (e8244388-e273-4aec-a9ff-856943866238) which are rejected by JoinQuant's web login form. The web login requires a Chinese mobile phone number.
  </what-built>
  <action>
    The user must update .env with valid JoinQuant web platform credentials:

    1. Open the file `.env` in the project root
    2. Replace the current JQ_USERNAME value (UUID format) with your Chinese mobile phone number registered at joinquant.com
    3. Replace the current JQ_PASSWORD value with your JoinQuant web platform password
    4. Save the file

    The .env file should look like:
    ```
    JQ_USERNAME=13800000000
    JQ_PASSWORD=YourWebPassword
    ```

    Note: The UUID credentials (e8244388-...) are JQData local API keys -- a completely different auth system from the web platform login.
  </action>
  <how-to-verify>
    After updating .env, confirm the format is correct:
    - JQ_USERNAME should be an 11-digit Chinese mobile phone number (starts with 1)
    - JQ_PASSWORD should be your web platform password (not the UUID)
  </how-to-verify>
  <resume-signal>Type "credentials updated" when .env has been updated with valid phone-number credentials</resume-signal>
</task>

<task type="auto">
  <name>Task 2: End-to-end pipeline run and selector fix-up</name>
  <read_first>
    - scraper/strategies.py
    - run_scrape.py
    - auth.py
    - .env
    - .planning/phases/01-scraper-schema/SELECTORS.md
  </read_first>
  <files>scraper/strategies.py</files>
  <action>
    Run the full pipeline end-to-end and fix any selector issues discovered against the live authenticated DOM.

    Step 1 -- Delete stale session and database to force fresh state:
    ```bash
    rm -f auth.json jq_knowledge.db
    ```

    Step 2 -- Run the scraper pipeline:
    ```bash
    uv run python run_scrape.py
    ```

    Step 3 -- Evaluate the output:

    A) If strategies table has >0 rows: the multi-selector fallback in strategies.py found working selectors. Record which selectors worked in a comment at the top of strategies.py.

    B) If strategies table has 0 rows but login succeeded (no "Redirected to login page" warning):
       - The sidebar selectors in strategies.py need fixing
       - Run a headed discovery session to inspect the real post-login strategy page DOM:
         ```python
         from playwright.sync_api import sync_playwright
         from auth import ensure_authenticated, AUTH_FILE
         p = sync_playwright().start()
         browser = p.chromium.launch(headless=False, slow_mo=500)
         context = browser.new_context(storage_state=str(AUTH_FILE))
         page = context.new_page()
         page.goto("https://www.joinquant.com/algorithm/study/index")
         # Inspect DOM manually, then:
         html = page.content()
         with open(".planning/phases/01-scraper-schema/strategy_live.html", "w") as f:
             f.write(html)
         ```
       - Parse the saved HTML to identify correct selectors for:
         - Sidebar container: update _SIDEBAR_SELECTORS in strategies.py
         - Category items: update _CATEGORY_SELECTORS
         - Strategy links: update _STRATEGY_LINK_SELECTORS
         - Code content on detail page: update _CODE_CONTENT_SELECTORS
         - Description: update _DESCRIPTION_SELECTORS
       - Update strategies.py with confirmed selectors (replace fallback arrays with confirmed selectors as first entries)
       - Re-run: `uv run python run_scrape.py`

    C) If login failed (redirect to login or phone number error): STOP and report -- the credentials provided in Task 1 are incorrect.

    Step 4 -- Verify idempotency by running a second time:
    ```bash
    uv run python run_scrape.py
    ```
    Compare row counts from first and second run -- they must be identical.

    Step 5 -- Verify session reuse: the second run should NOT navigate to the login URL. Check the output for absence of "Logging in" or login-related messages.

    Step 6 -- Run all tests to confirm no regressions:
    ```bash
    uv run pytest tests/ -x -q
    ```
    All 29 tests must still pass.
  </action>
  <verify>
    <automated>uv run python -c "
import sqlite3
conn = sqlite3.connect('jq_knowledge.db')
api = conn.execute('SELECT COUNT(*) FROM api_docs').fetchone()[0]
params = conn.execute('SELECT COUNT(*) FROM api_params').fetchone()[0]
strats = conn.execute('SELECT COUNT(*) FROM strategies').fetchone()[0]
print(f'api_docs={api} api_params={params} strategies={strats}')
assert api > 0, f'api_docs has {api} rows'
assert strats > 0, f'strategies has {strats} rows'
print('PASS: All tables populated')
conn.close()
"</automated>
  </verify>
  <acceptance_criteria>
    - jq_knowledge.db exists and is non-empty
    - `SELECT COUNT(*) FROM api_docs` returns > 0
    - `SELECT COUNT(*) FROM strategies` returns > 0
    - `SELECT COUNT(*) FROM api_params` returns > 0
    - Running `uv run python run_scrape.py` twice produces identical row counts (idempotency)
    - auth.json exists after first run
    - Second run does not re-execute login flow (session reuse confirmed)
    - `uv run pytest tests/ -x -q` exits 0 with 29 tests passing
    - scraper/strategies.py contains confirmed selectors (not just inferred fallback arrays)
  </acceptance_criteria>
  <done>
    jq_knowledge.db contains non-zero rows in api_docs, api_params, api_return_attrs, and strategies. Second run is idempotent with identical row counts. auth.json session is reused on second run. All 29 tests pass. Strategy selectors confirmed against live DOM.
  </done>
</task>

</tasks>

<verification>
Phase 1 gap closure is complete when:
1. `uv run python run_scrape.py` completes without error
2. All 4 SQLite tables have non-zero row counts
3. Strategy rows contain name, category, code_content populated from live scrape
4. Second run produces identical row counts (idempotent)
5. Second run reuses auth.json (no re-login)
6. All 29 existing tests pass
</verification>

<success_criteria>
- jq_knowledge.db exists with non-zero rows in api_docs AND strategies tables
- Idempotent: second run produces identical row counts
- Session reuse: auth.json loaded on second run, no login flow executed
- All 29 tests pass after any selector updates
- VERIFICATION.md gaps resolved: both failed truths now pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-scraper-schema/01-05-SUMMARY.md`
</output>
